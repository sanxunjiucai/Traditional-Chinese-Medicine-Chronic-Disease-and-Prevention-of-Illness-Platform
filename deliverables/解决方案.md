# 中医慢病与治未病平台 — 解决方案

> 版本：V1.0 · 2026-03-03
> 适用场景：三甲医院治未病科/慢病管理中心信息化招标、院内自研落地

---

## 一、背景与目标

### 1.1 行业背景

- 我国慢性病患者超 4 亿，高血压、糖尿病是核心负担；中医治未病体系被纳入基本公共卫生服务。
- 现有痛点：**健康档案孤岛**（体检、HIS、随访数据割裂）、**依从性低**（打卡与随访脱节）、**中医特色弱**（体质辨识无法与慢病闭环联动）。

### 1.2 建设目标

| 目标维度 | 具体指标 |
|---|---|
| 闭环覆盖 | 建档 → 体质辨识 → 调护建议 → 随访打卡 → 预警处置 全链路贯通 |
| 数据合规 | 操作全链路审计日志、隐私授权版本化、RBAC 权限最小化 |
| 可扩展 | 病种/指标/模板均为字典驱动，支持 V2/V3 扩容 |
| 可演示 | DEMO_MODE 自动 Seed，10 分钟内可本地启动展示 |

---

## 二、整体架构

```
┌──────────────────────────────────────────────────────────┐
│                      用户端（H5）                          │
│  首页 · 档案 · 指标录入 · 体质辨识 · 随访打卡 · 内容库     │
└────────────────────┬─────────────────────────────────────┘
                     │ HTTPS / Cookie JWT
┌────────────────────▼─────────────────────────────────────┐
│                   FastAPI 服务层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ auth     │ │ health   │ │constitu- │ │  followup   │ │
│  │ tools    │ │ tools    │ │tion tools│ │  tools      │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                  │
│  │ alert    │ │ content  │ │ audit    │                  │
│  │ tools    │ │ tools    │ │ tools    │                  │
│  └──────────┘ └──────────┘ └──────────┘                  │
│  ┌────────────────────────────────────────────────────┐  │
│  │ Services 层：评分引擎 · 推荐引擎 · 预警引擎 · 审计  │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────┘
                     │ SQLAlchemy (asyncpg)
┌────────────────────▼─────────────────────────────────────┐
│              PostgreSQL（生产）/ SQLite（测试）             │
└──────────────────────────────────────────────────────────┘
┌──────────────────────────────────────────────────────────┐
│               管理端 PC（Jinja2 SSR）                      │
│       预警处置台 · 内容审核发布 · 审计日志查询              │
└──────────────────────────────────────────────────────────┘
```

---

## 三、核心功能模块

### 3.1 用户与合规基础

- **认证**：手机号 + 密码登录，HttpOnly Cookie JWT，`access_token_expire` 可配置
- **隐私授权**：`ConsentRecord` 版本化记录，含同意时间与渠道
- **RBAC**：`PATIENT` / `PROFESSIONAL` / `ADMIN` 三级角色，接口级权限拦截
- **审计日志**：16 类关键 action 自动写入，支持时间范围 + 用户 + 动作筛选

### 3.2 健康档案与慢病管理

- **个人健康档案**：基础信息、生活方式（吸烟/饮酒/运动/睡眠/压力）、Upsert 语义
- **慢病专病档案**：可扩展病种字典（V1 支持高血压、2型糖尿病）、用药记录、目标值
- **指标采集**：血压（收缩压/舒张压）、血糖、体重、腰围；支持场景标注（晨起/餐后）
- **指标联动预警**：录入即触发预警引擎，无额外调用

### 3.3 中医治未病（体质辨识）

- **九种体质问卷**：60 道题、版本化题库、反向题处理
- **评分算法**：原始分 → 百分制转化分，主导体质 + 兼夹体质
- **状态机**：`ANSWERING → SUBMITTED → REPORTED`，断点续答
- **联动调护建议**：提交即自动生成个性化 RecommendationPlan

### 3.4 调护建议（模板化）

- **五大类别**：起居作息 / 饮食调护 / 运动导引 / 情志调护 / 外治非药
- **匹配逻辑**：体质类型 + 病种联合匹配模板库
- **版本管理**：生成新方案时，旧方案自动归档（ARCHIVED）

### 3.5 随访管理（慢病闭环）

- **随访计划**：按病种匹配 30 天模板，自动生成每日任务（指标上报/运动/用药/饮食/睡眠）
- **打卡系统**：CheckIn 状态机（PENDING → DONE/MISSED）
- **依从性计算**：`完成任务数 / 总应完成任务数`
- **权限隔离**：用户只能访问自己的计划与打卡记录

### 3.6 预警与处置

- **预警规则**（可配置）：HIGH/MEDIUM/LOW 三级阈值，数据库驱动
  - 血压：收缩压 ≥180（HIGH）/ ≥160（MEDIUM）/ ≥140（LOW）
  - 血糖：≥11.1（HIGH）/ ≥8.0（MEDIUM）/ ≥7.0（LOW）
- **事件状态机**：`OPEN → ACKED → CLOSED`
- **管理端处置台**：按 severity/status 筛选，记录 handler_note

### 3.7 内容库（治未病科普）

- **状态机**：`DRAFT → PENDING_REVIEW → PUBLISHED → OFFLINE`
- **分角色视图**：患者只见 PUBLISHED；管理员全量管理
- **标签体系**：支持体质/病种/饮食/运动等多标签过滤

---

## 四、技术选型

| 层 | 技术 | 理由 |
|---|---|---|
| Web 框架 | FastAPI 0.115 | 异步原生、自动 OpenAPI、高性能 |
| ORM | SQLAlchemy 2.0 asyncio | 异步 session，类型安全 |
| 数据库 | PostgreSQL（生产）/ SQLite（测试） | 生产高并发，测试零依赖 |
| 模板渲染 | Jinja2 SSR | 无需前端构建，快速可演示 |
| 认证 | JWT + HttpOnly Cookie | CSRF 防护，无 XSS 泄漏风险 |
| 密码加密 | bcrypt（passlib） | 行业标准，抗彩虹表 |
| 测试 | pytest-asyncio + httpx | 全异步集成测试 |
| 迁移 | Alembic | 版本化 DDL，生产安全 |
| 容器 | Docker Compose | 一键拉起 pg + app |

---

## 五、数据模型概览

```
User (id, phone, name, role, password_hash, is_active)
  ├── ConsentRecord (version, ip, channel, created_at)
  ├── HealthProfile (gender, birth_date, height, weight, lifestyle...)
  ├── ChronicDiseaseRecord (disease_type, medications, target_values)
  ├── HealthIndicator (type, values, scene, recorded_at)
  ├── ConstitutionAssessment (status, main_type, result)
  │     └── ConstitutionAnswer (question_id, answer_value)
  ├── RecommendationPlan (main_type, status, items[])
  ├── FollowupPlan (disease_type, status, start/end_date)
  │     ├── FollowupTask (name, type, required, scheduled_date)
  │     └── CheckIn (status, value, checked_at)
  └── AlertEvent (rule_id, severity, status, trigger_value)

ConstitutionQuestion (code, body_type, seq, content, options, is_reverse)
AlertRule (indicator_type, severity, operator, threshold)
FollowupTemplate (disease_type, duration_days, tasks[])
RecommendationTemplate (body_type, disease_type, category, content)
ContentItem (title, body, tags, status, author_id, published_at)
AuditLog (action, resource_type, resource_id, user_id, ip, old/new_values)
```

---

## 六、安全合规设计

| 方面 | 实现 |
|---|---|
| 传输安全 | HTTPS（生产），Cookie SameSite=Lax + HttpOnly |
| 存储安全 | 密码 bcrypt，JWT HS256 密钥从环境变量注入 |
| 权限最小化 | 每个接口声明最低角色要求，deps 层统一校验 |
| 数据审计 | 关键操作全量写 AuditLog（action + old/new values + IP） |
| 隐私授权 | ConsentRecord 版本化，含时间戳与渠道 |
| 数据隔离 | 患者只能访问自己数据，越权返回 404（不暴露存在性） |

---

## 七、部署与启动

### 本地开发

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量（复制示例）
cp .env.example .env

# 3. 数据库迁移
alembic upgrade head

# 4. 启动（DEMO_MODE 自动 Seed 演示数据）
DEMO_MODE=true uvicorn app.main:app --reload
```

### Docker 一键启动

```bash
docker-compose up -d
# 访问：http://localhost:8000
```

### 关键地址

| 地址 | 说明 |
|---|---|
| `http://localhost:8000/login` | 登录页 |
| `http://localhost:8000/h5/home` | 患者首页 |
| `http://localhost:8000/gui/admin/alerts` | 管理端预警台 |
| `http://localhost:8000/api/docs` | Swagger 交互文档 |
| `http://localhost:8000/healthz` | 健康检查 |

### 演示账号（Seed 数据）

| 角色 | 手机号 | 密码 |
|---|---|---|
| 患者 | 13800000001 | demo1234 |
| 管理员 | 13900000001 | admin1234 |

---

## 八、V2/V3 扩展路线

### V2（专业端增强）
- 医生工作台：随访点评、计划调整、质控报表
- 更多病种：高脂血症、痛风、慢阻肺
- 复评体系：30/90 天体质复评提醒与对比
- 人群统计：按科室/病种的达标率与预警率报表

### V3（全量对接）
- HIS/LIS 集成：患者信息同步、检验结果导入
- 设备接入：蓝牙血压计/血糖仪云平台 API
- 消息推送：短信/微信模板消息随访提醒
- 多机构 RBAC：多院区、数据隔离
- AI 辅助：舌象识别（严格标注非诊断）

---

## 九、风险与合规提示

1. **处方禁止**：V1 仅输出「调护建议」，不涉及处方、方药推荐；如需处方须接入医生端开立流程并满足互联网诊疗规范。
2. **设备数据**：接入第三方设备平台需落实用户授权与数据脱敏。
3. **AI 功能**：舌象识别等 AI 辅助功能须标注「仅供参考，不构成诊断」，建议 V3 灰度上线。
4. **数据留存**：按《网络安全法》《个人信息保护法》，健康数据建议留存 ≥5 年，审计日志 ≥3 年。
